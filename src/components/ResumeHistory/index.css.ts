import { style } from '@vanilla-extract/css';

export const resumeHistoryTitleStyle = style({
  textShadow: '0px 2px 4px #777',
  fontWeight: 'bold',
  fontSize: '1.2em',
});

export const resumeHistoryPeriodStyle = style({
  fontSize: '0.9em',
});
export const resumeHistoryContentStyle = style({});
